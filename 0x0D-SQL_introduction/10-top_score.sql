-- Script that lists all records of the table second_table in your MySQL server.
-- Results should display both the score and the name.
SELECT `score`, `name`
FROM `second_table`
ORDER BY `score` DESC;
